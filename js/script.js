// setInterval(()=>{
    // if(!clock.paused){
        // clock.update()
        // clock.count
        // console.log()
        // console.log(clock.getCurrentTimeRegressed())
        // console.log(clock.getFullTime())
        // console.log(clock.mode)
        // screenApp.setCircle(clock.getCurrentTimeRegressed(),clock.getFullTime())


        // screenApp.setCircle(clock.getCurrentTimeRegressed(),100)

    // }
// },1000/60)